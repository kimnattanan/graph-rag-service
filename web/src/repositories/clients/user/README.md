# graph_rag_service_user

GraphRagServiceUser - JavaScript client for graph_rag_service_user
Authentication, authorization, and user management.
This SDK is automatically generated by the [OpenAPI Generator](https://openapi-generator.tech) project:

- API version: 1.0.0
- Package version: 1.0.0
- Build package: org.openapitools.codegen.languages.JavascriptClientCodegen

## Installation

### For [Node.js](https://nodejs.org/)

#### npm

To publish the library as a [npm](https://www.npmjs.com/), please follow the procedure in ["Publishing npm packages"](https://docs.npmjs.com/getting-started/publishing-npm-packages).

Then install it via:

```shell
npm install graph_rag_service_user --save
```

Finally, you need to build the module:

```shell
npm run build
```

##### Local development

To use the library locally without publishing to a remote npm registry, first install the dependencies by changing into the directory containing `package.json` (and this README). Let's call this `JAVASCRIPT_CLIENT_DIR`. Then run:

```shell
npm install
```

Next, [link](https://docs.npmjs.com/cli/link) it globally in npm with the following, also from `JAVASCRIPT_CLIENT_DIR`:

```shell
npm link
```

To use the link you just defined in your project, switch to the directory you want to use your graph_rag_service_user from, and run:

```shell
npm link /path/to/<JAVASCRIPT_CLIENT_DIR>
```

Finally, you need to build the module:

```shell
npm run build
```

#### git

If the library is hosted at a git repository, e.g.https://github.com/GIT_USER_ID/GIT_REPO_ID
then install it via:

```shell
    npm install GIT_USER_ID/GIT_REPO_ID --save
```

### For browser

The library also works in the browser environment via npm and [browserify](http://browserify.org/). After following
the above steps with Node.js and installing browserify with `npm install -g browserify`,
perform the following (assuming *main.js* is your entry file):

```shell
browserify main.js > bundle.js
```

Then include *bundle.js* in the HTML pages.

### Webpack Configuration

Using Webpack you may encounter the following error: "Module not found: Error:
Cannot resolve module", most certainly you should disable AMD loader. Add/merge
the following section to your webpack config:

```javascript
module: {
  rules: [
    {
      parser: {
        amd: false
      }
    }
  ]
}
```

## Getting Started

Please follow the [installation](#installation) instruction and execute the following JS code:

```javascript
var GraphRagServiceUser = require('graph_rag_service_user');

var defaultClient = GraphRagServiceUser.ApiClient.instance;
// Configure Bearer (JWT) access token for authorization: bearerAuth
var bearerAuth = defaultClient.authentications['bearerAuth'];
bearerAuth.accessToken = "YOUR ACCESS TOKEN"

var api = new GraphRagServiceUser.DefaultApi()
var createUserRequest = new GraphRagServiceUser.CreateUserRequest(); // {CreateUserRequest} 
var callback = function(error, data, response) {
  if (error) {
    console.error(error);
  } else {
    console.log('API called successfully. Returned data: ' + data);
  }
};
api.createUser(createUserRequest, callback);

```

## Documentation for API Endpoints

All URIs are relative to *https://localhost/api*

Class | Method | HTTP request | Description
------------ | ------------- | ------------- | -------------
*GraphRagServiceUser.DefaultApi* | [**createUser**](docs/DefaultApi.md#createUser) | **POST** /users | Create a user (admin only)
*GraphRagServiceUser.DefaultApi* | [**deleteUser**](docs/DefaultApi.md#deleteUser) | **DELETE** /users/{userId} | Delete a user (admin only)
*GraphRagServiceUser.DefaultApi* | [**getCurrentUser**](docs/DefaultApi.md#getCurrentUser) | **GET** /users/me | Get the authenticated user profile
*GraphRagServiceUser.DefaultApi* | [**getCurrentUserPermissions**](docs/DefaultApi.md#getCurrentUserPermissions) | **GET** /users/me/permissions | Get permissions for the authenticated user
*GraphRagServiceUser.DefaultApi* | [**getUser**](docs/DefaultApi.md#getUser) | **GET** /users/{userId} | Get a user by ID (admin only)
*GraphRagServiceUser.DefaultApi* | [**listUsers**](docs/DefaultApi.md#listUsers) | **GET** /users | List users (admin only)
*GraphRagServiceUser.DefaultApi* | [**login**](docs/DefaultApi.md#login) | **POST** /auth/login | Authenticate and receive access tokens
*GraphRagServiceUser.DefaultApi* | [**refreshToken**](docs/DefaultApi.md#refreshToken) | **POST** /auth/refresh | Exchange a refresh token for a new access token
*GraphRagServiceUser.DefaultApi* | [**register**](docs/DefaultApi.md#register) | **POST** /auth/register | Register a new user account
*GraphRagServiceUser.DefaultApi* | [**updateUser**](docs/DefaultApi.md#updateUser) | **PUT** /users/{userId} | Update a user (admin only)


## Documentation for Models

 - [GraphRagServiceUser.AuthResponse](docs/AuthResponse.md)
 - [GraphRagServiceUser.CreateUserRequest](docs/CreateUserRequest.md)
 - [GraphRagServiceUser.Error](docs/Error.md)
 - [GraphRagServiceUser.LoginRequest](docs/LoginRequest.md)
 - [GraphRagServiceUser.Permissions](docs/Permissions.md)
 - [GraphRagServiceUser.RefreshRequest](docs/RefreshRequest.md)
 - [GraphRagServiceUser.RegisterRequest](docs/RegisterRequest.md)
 - [GraphRagServiceUser.UpdateUserRequest](docs/UpdateUserRequest.md)
 - [GraphRagServiceUser.User](docs/User.md)
 - [GraphRagServiceUser.UserList](docs/UserList.md)


## Documentation for Authorization



### bearerAuth

- **Type**: Bearer authentication (JWT)

